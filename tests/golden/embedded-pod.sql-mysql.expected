-- Generated by h5cpp-compiler SQL backend
-- Dialect: mysql

CREATE TABLE IF NOT EXISTS `sn__Inner` (
    `a` INT NOT NULL,
    `b` DOUBLE NOT NULL
);

CREATE TABLE IF NOT EXISTS `sn__Outer` (
    `idx` INT NOT NULL,
    `inner_singleton` JSON NOT NULL,
    `inner_array` JSON NOT NULL,
    `field_02` JSON NOT NULL
);
