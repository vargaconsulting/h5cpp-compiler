#pragma once

/* Generated by h5cpp-compiler Avro backend */

#include <cstddef>
#include <cstdint>

namespace avro::meta {

enum class avro_type_t : std::uint8_t {
    null, boolean, int32, int64, float32, float64,
    bytes, string, array, map, object, optional, fixed,
    date, time_millis, time_micros,
    timestamp_millis, timestamp_micros,
    local_timestamp_millis, local_timestamp_micros,
    decimal, uuid
};

struct field_desc {
    const char*   json_name;
    avro_type_t   type;
    std::size_t   offset;
    bool          required;
    const char*   doc;
    const field_desc* item;
    const field_desc* key;
    const field_desc* value;
    const char*   fixed_name;
    std::size_t   fixed_size;
    std::uint8_t  decimal_precision;
    std::uint8_t  decimal_scale;
};

template<typename T>
struct descriptor {
    static constexpr char alias[] = "";
    static constexpr field_desc fields[] = {};
    static constexpr std::size_t field_count = 0;
};

} // namespace avro::meta

// descriptor for sn::typecheck::Record
template<>
struct descriptor<sn::typecheck::Record> {
    static constexpr field_desc fields[] = {
        { "price", avro_type_t::decimal, offsetof(sn::typecheck::Record, price), false, nullptr, nullptr, nullptr, nullptr, nullptr, 0, 10, 2 },
        { "tax", avro_type_t::decimal, offsetof(sn::typecheck::Record, tax), false, nullptr, nullptr, nullptr, nullptr, nullptr, 0, 5, 0 },
        { "name", avro_type_t::string, offsetof(sn::typecheck::Record, name), false, nullptr, nullptr, nullptr, nullptr, nullptr, 0, 0, 0 }
    };
    static constexpr std::size_t field_count = 3;
};

